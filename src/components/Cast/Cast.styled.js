import styled from 'styled-components';

const CastList = styled.ul`
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
`;

const Name = styled.p`
  font-size: 20px;
  font-weight: 600;
`;

export { CastList, Name };
